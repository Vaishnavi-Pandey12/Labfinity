"""Backend package init.

This file exists so that ``backend`` can be imported as a package when the
API is started from the workspace root (using ``uvicorn backend.main:app``).
"""
# marker file for package
